# MT4 to MT5 Trade Copier Configuration
# Copy this file to .env and update with your values
# DO NOT commit .env to git - it contains sensitive information

# File paths and MT5 connection settings
MT4_FILE_PATH=C:\Users\Administrator\AppData\Roaming\MetaQuotes\Terminal\Common\Files\mt4_trades.csv
MT5_LOGIN=511055954
MT5_PASSWORD=eW!p9*
MT5_SERVER=FTMO-Server
POLL_INTERVAL_SEC=1
USE_FILE_WATCHER=true

# Symbol mapping (JSON format)
# Example: {"GER40": "DE40.cash"}
SYMBOL_MAP={}

# Price tolerance in points (JSON format)
# Example: {"GER40": 5.0}
PRICE_TOLERANCE={}

# Entry price tolerance as absolute price difference (JSON format)
# Example: {"GER40.cash": 5.0, "EURUSD": 0.0005}
ENTRY_PRICE_TOLERANCE={"GER40.cash": 5.0, "EURUSD": 0.0005, "XAUUSD": 3.0, "USJPY": 0.05, "BTCUSD": 200.0, "JP225.cash": 20.0}

# Entry price tolerance multiplier (JSON format)
# Example: {"GER40.cash": 2.0}
ENTRY_PRICE_TOLERANCE_BETTER_PRICE_MULTIPLIER={}
ENTRY_PRICE_TOLERANCE_BETTER_PRICE_MULTIPLIER_DEFAULT=2.0

# Limit order settings
USE_LIMIT_ORDERS_ON_TOLERANCE_FAIL=false
USE_SECONDARY_LIMIT_ORDER=false
SECONDARY_LIMIT_ORDER_LOT_PERCENTAGE=0.5
SECONDARY_LIMIT_ORDER_DISTANCE_PERCENTAGE=0.5

# Lot multiplier settings
GLOBAL_LOT_MULTIPLIER=1.0
# Symbol lot multiplier (JSON format)
# Example: {"GER40": 2.0}
LOT_MULTIPLIER={}
# Magic number lot multiplier (JSON format, keys must be strings)
# Example: {"123456": 1.5, "789012": 0.5}
MAGIC_LOT_MULTIPLIER={}

# Magic number filter (JSON array format)
# Example: [123456, 789012]
MAGIC_FILTER=[]

# Telegram settings
TELEGRAM_ENABLED=true
TELEGRAM_BOT_TOKEN=
